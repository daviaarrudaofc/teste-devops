name: Monitor Docker

on:
  push:

jobs:
  build-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v2

      - name: Construir Docker
        run: docker build -t monitor-devops .
